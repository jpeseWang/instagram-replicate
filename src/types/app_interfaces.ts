export interface IGifsData {
  data: string;
}
export interface User {
  id: string;
  fullname: string;
  avatar?: string;
}
export interface IUserData {
  email: string;
  username: string;
  passwords: string;
}
